import { GroupByStagePipe } from './group-by-stage.pipe';

describe('GroupByStagePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByStagePipe();
    expect(pipe).toBeTruthy();
  });
});
